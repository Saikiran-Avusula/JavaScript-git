// const cart = ["shoes", "pants", "shirts"];

// const promise = createOrder(cart);
// //get cart items, it calls to function createOrder(cart);
// // in this cart orderid found proceed to payment .ie., here,
// promise.then((orderId) => {
//     console.log(orderId);
// })

// function createOrder(cart) {
//     return new Promise((resolve, reject) => {
//         // create an order
//         // validate cart
//         // order Id
//         if (!validateCart(cart)) {
//             const error = new Error("cart is not validated !")
//             reject(error)
//         } else
//         //logic for creating order
//         {
//             const orderId = 123456;
//             return resolve("Your order id:" + orderId);

//         }
//     });

// }

// //it checks the validation of cart if any items present in it or not ?
// function validateCart(cart) {
//     return cart;
// }









